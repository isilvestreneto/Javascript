// Cap 4: Data

// Manipulate Arrays
let sequence = [1, 2, 3];

sequence.push(4);
sequence.push(5); // push method adds values to the end of an array
console.log(sequence);

sequence.pop(); // does the opposite of push, removing the last value in the array
console.log(sequence);